{
  "name": "proyecto-noticias",
  "type": "module",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "sqlite3"
    ]
  },
  "scripts": {
    "build": "nuxt build",
    "predev": "node scripts/sync-opciones-categorias.mjs",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "sync:categorias": "node scripts/sync-opciones-categorias.mjs",
    "postinstall": "node scripts/patch-nuxt-content-preview.mjs && node scripts/sync-opciones-categorias.mjs && nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^3.12.0",
    "nuxt": "^4.4.2",
    "nuxt-studio": "1.5.1",
    "vue": "^3.5.30",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "zod": "^4.3.6",
    "zod-to-json-schema": "^3.25.1"
  }
}
